<div class= "container ">
  <div class= "row">
    <div class= "col col-xs-12 col-sm-6 mb50">
      <div class = "images">
      <%= image_tag @item.cat_image, alt: 'cat', class: "img-responsive"%>

      </div>
    </div>
    <div class="col col-xs-12 col-sm-6 mb50">
      <div class="summary entry-summary">
        <div>
          <h1><%= @item.title%></h1>
          <p class = "price">
            <ins>
              <span class="amount"><%=@item.price%> €</span>
            </ins>
          </p>
        </div>
        <div class="lead">
          <p><%= @item.description%></p>
          <%= button_to 'Ajouter au panier', item_carts_path(item_id: @item), class: 'btn btn-primary' %>
        </div>
          <%= render 'add_to_cart', item: @item, user: @user %>
      </div>
    </div>
  </div>
</div>
