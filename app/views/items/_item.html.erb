<div class= "container ">
  <div class= "row">
    <div class= "col col-xs-12 col-sm-6 mb50">
      <div class = "images">
        <img src="<%= @item.image_url %>" class="img-responsive" alt="..." >
      </div>
    </div>
    <div class="col col-xs-12 col-sm-6 mb50">
      <div class="summary entry-summary">
        <div>
          <h1><%= @item.title%></h1>
          <p class = "price">
            <ins>
              <span class="amount"><%=@item.price%> €</span>
            </ins>
          </p>
        </div>
        <div class="lead">
          <p><%= @item.description%></p>
          <%= button_to 'Add to cart', item_carts_path(item_id: @item), class: 'button is-warning add-to-cart' %>
        </div>
      </div>
    </div>
  </div>
</div>
